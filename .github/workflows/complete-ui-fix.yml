name: Complete UI and Theme Fix (legacy)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled; fix-logo-fit.yml is the authoritative UI workflow."
