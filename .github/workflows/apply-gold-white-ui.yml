name: Apply Leeway gold-white UI

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Gold-white UI changes are now maintained by the unified UI workflow."
